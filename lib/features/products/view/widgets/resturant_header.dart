import 'package:flutter/widgets.dart';

class ResturantHeader extends StatelessWidget {
  const ResturantHeader({super.key});

  

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [
        Text(""),
      ],
    );
  }
}
